apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "__CHART__.fullname" . }}-test-connection"
  labels:
{{ include "__CHART__.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "__CHART__.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
